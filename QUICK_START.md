# 🚀 دليل البدء السريع

## التثبيت السريع

### 1. تثبيت الحزم
```bash
npm run install-all
```

### 2. إعداد MongoDB
تأكد من أن MongoDB يعمل على جهازك، أو استخدم MongoDB Atlas.

### 3. إعداد ملف البيئة
```bash
cd backend
cp .env.example .env
```

قم بتعديل ملف `.env`:
```
PORT=5000
MONGODB_URI=mongodb://localhost:27017/les-rois-des-bois
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRE=7d
NODE_ENV=development
```

### 4. إنشاء حساب مدير
```bash
cd backend
npm run create-admin
```

سيتم إنشاء حساب مدير افتراضي:
- **البريد الإلكتروني:** admin@lesroisdesbois.com
- **كلمة المرور:** admin123

⚠️ **مهم:** قم بتغيير كلمة المرور بعد أول تسجيل دخول!

### 5. تشغيل المشروع
```bash
# من المجلد الرئيسي
npm run dev
```

سيتم تشغيل:
- **Backend:** http://localhost:5000
- **Frontend:** http://localhost:3000

### 6. تسجيل الدخول
افتح المتصفح على: http://localhost:3000

استخدم بيانات المدير الافتراضية:
- البريد: `admin@lesroisdesbois.com`
- كلمة المرور: `admin123`

## 📋 الخطوات التالية

1. ✅ سجل دخول إلى لوحة التحكم
2. ✅ أضف فئات للمنتجات
3. ✅ أضف منتجات عادية
4. ✅ أضف متغيرات للمنتجات (ألوان، مواد، إلخ)
5. ✅ أنشئ منتجات خاصة من منتجين عاديين

## 🎯 استخدام المنتجات الخاصة

### مثال عملي:

1. **أنشئ منتج "سطح الطاولة"** مع متغيرات:
   - أحمر
   - أخضر
   - أزرق

2. **أنشئ منتج "أرجل الطاولة"** مع متغيرات:
   - معدني
   - خشبي

3. **أنشئ منتج خاص "طاولة كاملة"**:
   - اختر "سطح الطاولة" كمنتج أساسي 1
   - اختر "أرجل الطاولة" كمنتج أساسي 2
   - سيتم توليد 6 تركيبات تلقائياً (3 × 2)
   - ارفع صورة لكل تركيبة
   - احفظ المنتج الخاص

## 🔧 استكشاف الأخطاء

### MongoDB غير متصل
تأكد من أن MongoDB يعمل:
```bash
# Windows
net start MongoDB

# Linux/Mac
sudo systemctl start mongod
```

### خطأ في المنافذ
إذا كانت المنافذ 3000 أو 5000 مستخدمة، قم بتغييرها في:
- Frontend: `frontend/vite.config.js`
- Backend: `backend/.env`

### مشاكل في رفع الصور
تأكد من وجود مجلدات الرفع:
```bash
cd backend
mkdir -p uploads/categories uploads/products uploads/special-products uploads/general
```

## 📞 الدعم

للمزيد من المعلومات، راجع ملف `README.md`.

